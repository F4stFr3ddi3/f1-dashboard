{
  "name": "f1-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "F1 standings, schedule, and results dashboard built on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
